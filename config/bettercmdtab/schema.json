{
  "$schema" : "https:\/\/json-schema.org\/draft\/2020-12\/schema",
  "additionalProperties" : true,
  "description" : "Generated by BetterCmdTab 26.7. Keys mirror the Switcher.* preferences (prefix stripped); unknown keys are ignored, absent keys keep their current value.",
  "properties" : {
    "$schema" : {
      "description" : "Location of this schema. Ignored as a setting.",
      "type" : "string"
    },
    "animationsEnabled" : {
      "description" : "Glide the panel, the tiles and the tab strip between states. Off, or with macOS Reduce Motion on, every change is a hard cut.",
      "type" : "boolean"
    },
    "appExceptions" : {
      "description" : "Per-app rules. An app with no entry follows the global settings.",
      "items" : {
        "additionalProperties" : false,
        "properties" : {
          "bundleID" : {
            "description" : "Bundle identifier of the app this rule applies to.",
            "pattern" : "^[A-Za-z0-9.\\-_]+$",
            "type" : "string"
          },
          "hide" : {
            "description" : "Whether this app is hidden from the switcher.",
            "enum" : [
              "dontHide",
              "always",
              "whenNoWindows"
            ],
            "enumDescriptions" : [
              "Don't hide",
              "Always",
              "When no open windows"
            ],
            "type" : "string"
          },
          "ignore" : {
            "description" : "Whether the switcher shortcut is passed through to this app instead of opening the panel.",
            "enum" : [
              "never",
              "always",
              "whenFullscreen"
            ],
            "enumDescriptions" : [
              "Never",
              "Always",
              "When fullscreen"
            ],
            "type" : "string"
          }
        },
        "required" : [
          "bundleID"
        ],
        "type" : "object"
      },
      "type" : "array"
    },
    "applicationsOnly" : {
      "description" : "Show one entry per app instead of one per window.",
      "type" : "boolean"
    },
    "backdropMaterial" : {
      "description" : "Blur material behind the rows. Ignored by the macOS 26 glass backdrop.",
      "enum" : [
        "hud",
        "sidebar",
        "menu",
        "popover",
        "fullScreen",
        "underWindow"
      ],
      "enumDescriptions" : [
        "HUD (default)",
        "Sidebar",
        "Menu",
        "Popover",
        "Full Screen",
        "Under Window"
      ],
      "type" : "string"
    },
    "backtickReversesAppSwitching" : {
      "description" : "Use the window-switch shortcut (⌘`) to step backwards through apps.",
      "type" : "boolean"
    },
    "boldSelectedLabel" : {
      "description" : "Show the highlighted entry's title in bold.",
      "type" : "boolean"
    },
    "browserTabMRU" : {
      "description" : "Track browser tabs in the recency order.",
      "type" : "boolean"
    },
    "browserTabPreviews" : {
      "description" : "Show thumbnails for browser tabs in the Previews layout.",
      "type" : "boolean"
    },
    "browserTabRowLimit" : {
      "description" : "Cap on tab entries per browser window. 0 = unlimited; anything else is taken as 2…16.",
      "maximum" : 16,
      "minimum" : 0,
      "type" : "integer"
    },
    "clickOutsideToDismiss" : {
      "description" : "Click outside the panel to dismiss it without switching.",
      "type" : "boolean"
    },
    "commitSoundName" : {
      "description" : "macOS system sound played on commit. A custom sound file stays on the Mac that chose it.",
      "enum" : [
        "Basso",
        "Blow",
        "Bottle",
        "Frog",
        "Funk",
        "Glass",
        "Hero",
        "Morse",
        "Ping",
        "Pop",
        "Purr",
        "Sosumi",
        "Submarine",
        "Tink"
      ],
      "type" : "string"
    },
    "currentSpaceOnly" : {
      "description" : "Legacy \"only the current Space\" flag, kept in sync with spaceScope for older builds. Edit spaceScope instead.",
      "type" : "boolean"
    },
    "cycleTileWidths" : {
      "description" : "Repeating the tile-left\/right shortcut cycles the window through half → two-thirds → one-third width.",
      "type" : "boolean"
    },
    "directActivationBindings" : {
      "description" : "Bundle ID activated by each direct-activation slot, in slot order. An empty string leaves the slot unused.",
      "items" : {
        "pattern" : "^[A-Za-z0-9.\\-_]*$",
        "type" : "string"
      },
      "maxItems" : 9,
      "minItems" : 9,
      "type" : "array"
    },
    "displayMode" : {
      "description" : "Which monitor the switcher opens on.",
      "enum" : [
        "mouseCursor",
        "activeWindow",
        "mainDisplay"
      ],
      "enumDescriptions" : [
        "Monitor with the cursor",
        "Monitor with the active app",
        "Main display"
      ],
      "type" : "string"
    },
    "excludedBundleIDs" : {
      "description" : "Legacy always-hidden bundle IDs, folded into appExceptions at launch. Edit appExceptions instead.",
      "items" : {
        "pattern" : "^[A-Za-z0-9.\\-_]+$",
        "type" : "string"
      },
      "type" : "array"
    },
    "expandBrowserTabsAsWindows" : {
      "description" : "List browser tabs (Safari, Chromium) as separate entries.",
      "type" : "boolean"
    },
    "expandTabsAsWindows" : {
      "description" : "List native system tabs (Finder, Terminal, TextEdit, …) as separate entries.",
      "type" : "boolean"
    },
    "experimentalBrowserTabMRU" : {
      "description" : "Legacy browser-tab recency flag, kept in sync with browserTabMRU for older builds. Edit browserTabMRU instead.",
      "type" : "boolean"
    },
    "experimentalBrowserTabPreviews" : {
      "description" : "Legacy browser-tab-preview flag, kept in sync with browserTabPreviews for older builds. Edit browserTabPreviews instead.",
      "type" : "boolean"
    },
    "experimentalInstantSpaceSwitch" : {
      "description" : "Legacy instant-Space-switch flag, kept in sync with instantSpaceSwitch for older builds. Edit instantSpaceSwitch instead.",
      "type" : "boolean"
    },
    "experimentalLivePreviews" : {
      "description" : "Legacy live-preview flag, kept in sync with livePreviews for older builds. Edit livePreviews instead.",
      "type" : "boolean"
    },
    "experimentalSwipeTrigger" : {
      "description" : "Three-finger trackpad swipe.",
      "type" : "boolean"
    },
    "experimentalUnreadBadges" : {
      "description" : "Legacy unread-badge flag, read once to seed showUnreadBadges. Edit showUnreadBadges instead.",
      "type" : "boolean"
    },
    "fontFace" : {
      "description" : "Typeface used for names and titles.",
      "enum" : [
        "system",
        "rounded",
        "serif",
        "monospaced"
      ],
      "enumDescriptions" : [
        "System",
        "Rounded",
        "Serif",
        "Monospaced"
      ],
      "type" : "string"
    },
    "fontScale" : {
      "description" : "Size of the name\/title text, independent of the panel scale.",
      "enum" : [
        "extraSmall",
        "small",
        "standard",
        "large",
        "extraLarge"
      ],
      "enumDescriptions" : [
        "Extra small",
        "Small",
        "Default",
        "Large",
        "Extra large"
      ],
      "type" : "string"
    },
    "fuzzySearchEnabled" : {
      "description" : "Type to filter the switcher.",
      "type" : "boolean"
    },
    "fuzzySearchRankBestMatchFirst" : {
      "description" : "Sort search results by match quality instead of keeping the list order.",
      "type" : "boolean"
    },
    "gridMaxColumns" : {
      "description" : "Column cap for the grid layout. 0 = automatic (as many as fit).",
      "maximum" : 12,
      "minimum" : 0,
      "type" : "integer"
    },
    "gridSingleRow" : {
      "description" : "Keep the grid on one row, shrinking the icons to fit, instead of wrapping. Ignored while gridMaxColumns caps the columns.",
      "type" : "boolean"
    },
    "hapticOnCommit" : {
      "description" : "Tap the trackpad when the switcher commits a selection.",
      "type" : "boolean"
    },
    "hideAllExcludedBundleIDs" : {
      "description" : "Bundle IDs the \"hide all windows\" shortcut leaves visible.",
      "items" : {
        "pattern" : "^[A-Za-z0-9.\\-_]+$",
        "type" : "string"
      },
      "type" : "array"
    },
    "hideFromScreenSharing" : {
      "description" : "Keep the switcher panel out of screen recordings and shared screens.",
      "type" : "boolean"
    },
    "hideMenuBarIcon" : {
      "description" : "Hide the menu-bar icon.",
      "type" : "boolean"
    },
    "hoverActionsEnabled" : {
      "description" : "Show action buttons on the entry under the pointer.",
      "type" : "boolean"
    },
    "hoverShowClose" : {
      "description" : "Include \"close\" among the hover actions.",
      "type" : "boolean"
    },
    "hoverShowForceQuit" : {
      "description" : "Include \"force quit\" among the hover actions.",
      "type" : "boolean"
    },
    "hoverShowHide" : {
      "description" : "Include \"hide app\" among the hover actions.",
      "type" : "boolean"
    },
    "hoverShowMaximize" : {
      "description" : "Include \"maximize\" among the hover actions.",
      "type" : "boolean"
    },
    "hoverShowMinimize" : {
      "description" : "Include \"minimize\" among the hover actions.",
      "type" : "boolean"
    },
    "hoverShowQuit" : {
      "description" : "Include \"quit\" among the hover actions.",
      "type" : "boolean"
    },
    "instantSpaceSwitch" : {
      "description" : "Switch Spaces without the macOS animation.",
      "type" : "boolean"
    },
    "layoutMode" : {
      "description" : "Switcher layout: list rows, an icon grid, or window previews.",
      "enum" : [
        "list",
        "iconDock",
        "windowPreview"
      ],
      "enumDescriptions" : [
        "List",
        "Grid View",
        "Previews"
      ],
      "type" : "string"
    },
    "letterChainTimeoutMs" : {
      "description" : "How long a typed letter-jump prefix stays active before it expires (milliseconds).",
      "maximum" : 3000,
      "minimum" : 200,
      "type" : "integer"
    },
    "letterHintsEnabled" : {
      "description" : "Show the letter that jumps to each entry.",
      "type" : "boolean"
    },
    "listWidthPercent" : {
      "description" : "Width of list rows as a percentage of the automatic, screen-scaled width.",
      "maximum" : 100,
      "minimum" : 30,
      "type" : "integer"
    },
    "livePreviews" : {
      "description" : "Keep window previews refreshing while the panel is open. Costs CPU\/GPU; needs macOS 14 or later.",
      "type" : "boolean"
    },
    "mouseClickSelectionEnabled" : {
      "description" : "Click an entry to switch to it.",
      "type" : "boolean"
    },
    "mouseHoverSelectionEnabled" : {
      "description" : "Highlight the entry under the pointer.",
      "type" : "boolean"
    },
    "nextScopedShortcutID" : {
      "description" : "Internal counter for allocating scoped-shortcut ids. Never decreases, so a removed id is never reused. Don't edit.",
      "type" : "integer"
    },
    "panelAppearance" : {
      "description" : "Light or dark switcher, independent of the rest of macOS.",
      "enum" : [
        "system",
        "light",
        "dark"
      ],
      "enumDescriptions" : [
        "System",
        "Light",
        "Dark"
      ],
      "type" : "string"
    },
    "panelCornerRadius" : {
      "description" : "Panel corner radius in points. 0 = automatic (follows the panel size), -1 = square corners.",
      "maximum" : 40,
      "minimum" : -1,
      "type" : "integer"
    },
    "panelOpacity" : {
      "description" : "Opacity of the panel background, in percent.",
      "maximum" : 100,
      "minimum" : 30,
      "type" : "integer"
    },
    "panelScalePercent" : {
      "description" : "Overall size of the switcher panel, in percent. 100% matches the macOS switcher.",
      "maximum" : 150,
      "minimum" : 35,
      "type" : "integer"
    },
    "panelSize" : {
      "description" : "Legacy size preset, migrated to panelScalePercent at launch. Use panelScalePercent instead.",
      "enum" : [
        "small",
        "standard",
        "large"
      ],
      "type" : "string"
    },
    "pinnedBundleIDs" : {
      "description" : "Bundle IDs pinned to the front of the switcher, in the order they appear.",
      "items" : {
        "pattern" : "^[A-Za-z0-9.\\-_]+$",
        "type" : "string"
      },
      "type" : "array"
    },
    "previewTitleAlignment" : {
      "description" : "Where the title sits under each window-preview tile.",
      "enum" : [
        "leading",
        "center",
        "trailing"
      ],
      "enumDescriptions" : [
        "Left",
        "Center",
        "Right"
      ],
      "type" : "string"
    },
    "recentlyClosedLimit" : {
      "description" : "How many recently closed apps to offer. 0 disables them.",
      "maximum" : 50,
      "minimum" : 0,
      "type" : "integer"
    },
    "revealDelayMs" : {
      "description" : "How long the shortcut must be held before the panel appears — a quicker tap switches without showing it (milliseconds).",
      "maximum" : 500,
      "minimum" : 40,
      "type" : "integer"
    },
    "scopedShortcutList" : {
      "description" : "Scoped switcher shortcuts — each opens the panel already filtered to one subset of windows.",
      "items" : {
        "additionalProperties" : false,
        "properties" : {
          "id" : {
            "description" : "Stable id of this entry, as a decimal string. Keys its recorded trigger and its shortcutOverrides entry — never reused.",
            "pattern" : "^[0-9]+$",
            "type" : "string"
          },
          "name" : {
            "description" : "Name the trigger chord is recorded under (the chord itself is stored outside this file).",
            "type" : "string"
          },
          "scope" : {
            "description" : "Which windows the shortcut shows.",
            "enum" : [
              "allAppsAllSpaces",
              "allAppsCurrentSpace",
              "currentAppWindows",
              "minimizedOnly"
            ],
            "enumDescriptions" : [
              "All windows",
              "Windows on this Space",
              "Current app's windows",
              "Minimized windows"
            ],
            "type" : "string"
          }
        },
        "required" : [
          "id",
          "name"
        ],
        "type" : "object"
      },
      "type" : "array"
    },
    "scopedShortcutScopes" : {
      "description" : "Legacy fixed scoped-shortcut slots, migrated to scopedShortcutList. Edit scopedShortcutList instead.",
      "items" : {
        "enum" : [
          "allAppsAllSpaces",
          "allAppsCurrentSpace",
          "currentAppWindows",
          "minimizedOnly"
        ],
        "enumDescriptions" : [
          "All windows",
          "Windows on this Space",
          "Current app's windows",
          "Minimized windows"
        ],
        "type" : "string"
      },
      "maxItems" : 3,
      "minItems" : 3,
      "type" : "array"
    },
    "scrollReverseDirection" : {
      "description" : "Reverse the scroll direction.",
      "type" : "boolean"
    },
    "scrollToSwitch" : {
      "description" : "Move the selection by scrolling.",
      "type" : "boolean"
    },
    "searchDismissMode" : {
      "description" : "Whether searching keeps the switcher open after the modifier is released.",
      "enum" : [
        "holdModifier",
        "stayOpen"
      ],
      "enumDescriptions" : [
        "Hold ⌘",
        "Stay open until I choose"
      ],
      "type" : "string"
    },
    "searchExpandsBrowserTabs" : {
      "description" : "Search browser tab titles too.",
      "type" : "boolean"
    },
    "searchIncludesLaunchableApps" : {
      "description" : "Include installed apps that aren't running, so search can launch them.",
      "type" : "boolean"
    },
    "shiftTapStepsBackward" : {
      "description" : "Tap Shift while switching to step backwards.",
      "type" : "boolean"
    },
    "shortcutOverrides" : {
      "description" : "Per-shortcut overrides of the global settings. Each entry names its shortcut in \"target\"; every other field is optional and inherits the global value when absent.",
      "items" : {
        "additionalProperties" : true,
        "properties" : {
          "applicationsOnly" : {
            "description" : "Override: Show one entry per app instead of one per window.",
            "enum" : [
              "true",
              "false"
            ],
            "type" : "string"
          },
          "backdropMaterial" : {
            "description" : "Override: Blur material behind the rows. Ignored by the macOS 26 glass backdrop.",
            "enum" : [
              "hud",
              "sidebar",
              "menu",
              "popover",
              "fullScreen",
              "underWindow"
            ],
            "enumDescriptions" : [
              "HUD (default)",
              "Sidebar",
              "Menu",
              "Popover",
              "Full Screen",
              "Under Window"
            ],
            "type" : "string"
          },
          "boldSelectedLabel" : {
            "description" : "Override: Show the highlighted entry's title in bold.",
            "enum" : [
              "true",
              "false"
            ],
            "type" : "string"
          },
          "expandBrowserTabsAsWindows" : {
            "description" : "Override: List browser tabs (Safari, Chromium) as separate entries.",
            "enum" : [
              "true",
              "false"
            ],
            "type" : "string"
          },
          "fontFace" : {
            "description" : "Override: Typeface used for names and titles.",
            "enum" : [
              "system",
              "rounded",
              "serif",
              "monospaced"
            ],
            "enumDescriptions" : [
              "System",
              "Rounded",
              "Serif",
              "Monospaced"
            ],
            "type" : "string"
          },
          "fontScale" : {
            "description" : "Override: Size of the name\/title text, independent of the panel scale.",
            "enum" : [
              "extraSmall",
              "small",
              "standard",
              "large",
              "extraLarge"
            ],
            "enumDescriptions" : [
              "Extra small",
              "Small",
              "Default",
              "Large",
              "Extra large"
            ],
            "type" : "string"
          },
          "gridMaxColumns" : {
            "description" : "Override: Column cap for the grid layout. 0 = automatic (as many as fit). Accepted: 0…12. Written as a decimal string.",
            "pattern" : "^-?[0-9]+$",
            "type" : "string"
          },
          "layoutMode" : {
            "description" : "Override: Switcher layout: list rows, an icon grid, or window previews.",
            "enum" : [
              "list",
              "iconDock",
              "windowPreview"
            ],
            "enumDescriptions" : [
              "List",
              "Grid View",
              "Previews"
            ],
            "type" : "string"
          },
          "letterHintsEnabled" : {
            "description" : "Override: Show the letter that jumps to each entry.",
            "enum" : [
              "true",
              "false"
            ],
            "type" : "string"
          },
          "listWidthPercent" : {
            "description" : "Override: Width of list rows as a percentage of the automatic, screen-scaled width. Accepted: 30…100. Written as a decimal string.",
            "pattern" : "^-?[0-9]+$",
            "type" : "string"
          },
          "panelAppearance" : {
            "description" : "Override: Light or dark switcher, independent of the rest of macOS.",
            "enum" : [
              "system",
              "light",
              "dark"
            ],
            "enumDescriptions" : [
              "System",
              "Light",
              "Dark"
            ],
            "type" : "string"
          },
          "panelCornerRadius" : {
            "description" : "Override: Panel corner radius in points. 0 = automatic (follows the panel size), -1 = square corners. Accepted: -1…40. Written as a decimal string.",
            "pattern" : "^-?[0-9]+$",
            "type" : "string"
          },
          "panelOpacity" : {
            "description" : "Override: Opacity of the panel background, in percent. Accepted: 30…100. Written as a decimal string.",
            "pattern" : "^-?[0-9]+$",
            "type" : "string"
          },
          "panelScalePercent" : {
            "description" : "Override: Overall size of the switcher panel, in percent. 100% matches the macOS switcher. Accepted: 35…150. Written as a decimal string.",
            "pattern" : "^-?[0-9]+$",
            "type" : "string"
          },
          "panelSize" : {
            "description" : "Legacy size preset, read once and migrated to panelScalePercent.",
            "enum" : [
              "small",
              "standard",
              "large"
            ],
            "type" : "string"
          },
          "previewTitleAlignment" : {
            "description" : "Override: Where the title sits under each window-preview tile.",
            "enum" : [
              "leading",
              "center",
              "trailing"
            ],
            "enumDescriptions" : [
              "Left",
              "Center",
              "Right"
            ],
            "type" : "string"
          },
          "showApplicationNames" : {
            "description" : "Override: Show application names next to the icons.",
            "enum" : [
              "true",
              "false"
            ],
            "type" : "string"
          },
          "showHidden" : {
            "description" : "Override: Include apps hidden with ⌘H.",
            "enum" : [
              "true",
              "false"
            ],
            "type" : "string"
          },
          "showMinimized" : {
            "description" : "Override: Include minimized windows.",
            "enum" : [
              "true",
              "false"
            ],
            "type" : "string"
          },
          "showUnreadBadges" : {
            "description" : "Override: Show each app's Dock unread badge on its entry.",
            "enum" : [
              "true",
              "false"
            ],
            "type" : "string"
          },
          "showWindowless" : {
            "description" : "Override: Include running apps that have no open window.",
            "enum" : [
              "true",
              "false"
            ],
            "type" : "string"
          },
          "showWindowStatusIcons" : {
            "description" : "Override: Show the hidden, minimized, full-screen and no-window glyphs on each entry.",
            "enum" : [
              "true",
              "false"
            ],
            "type" : "string"
          },
          "showWindowTitleLabel" : {
            "description" : "Override: Show each window's title, not just its app.",
            "enum" : [
              "true",
              "false"
            ],
            "type" : "string"
          },
          "sortOrder" : {
            "description" : "Override: Order apps and windows are listed in.",
            "enum" : [
              "mru",
              "mruWindows",
              "alphabetical",
              "launchOrder"
            ],
            "enumDescriptions" : [
              "Most recent",
              "Most recent (windows)",
              "Alphabetical",
              "Launch order"
            ],
            "type" : "string"
          },
          "spaceScope" : {
            "description" : "Override: which Spaces windows are pulled from. \"inherit\" follows the global spaceScope.",
            "enum" : [
              "inherit",
              "currentSpace",
              "allSpaces",
              "visibleSpaces"
            ],
            "enumDescriptions" : [
              "Use global default",
              "This Space only",
              "All Spaces",
              "Visible Spaces"
            ],
            "type" : "string"
          },
          "stayOpenOnQuickTap" : {
            "description" : "Override: Also keep it open after a quick tap of the shortcut.",
            "enum" : [
              "true",
              "false"
            ],
            "type" : "string"
          },
          "stayOpenOnRelease" : {
            "description" : "Override: Keep the switcher open after the modifier is released.",
            "enum" : [
              "true",
              "false"
            ],
            "type" : "string"
          },
          "target" : {
            "description" : "Which shortcut this entry overrides.",
            "pattern" : "^(switchApps|switchWindows|scoped\\.[0-9]+)$",
            "type" : "string"
          },
          "titleTruncationMode" : {
            "description" : "Override: Which part of a too-long title is replaced by an ellipsis.",
            "enum" : [
              "head",
              "middle",
              "tail"
            ],
            "enumDescriptions" : [
              "Beginning",
              "Middle",
              "End"
            ],
            "type" : "string"
          }
        },
        "required" : [
          "target"
        ],
        "type" : "object"
      },
      "type" : "array"
    },
    "showApplicationNames" : {
      "description" : "Show application names next to the icons.",
      "type" : "boolean"
    },
    "showBrowserIconOnTabs" : {
      "description" : "Badge each browser-tab entry's favicon with the source browser's icon.",
      "type" : "boolean"
    },
    "showHiddenApps" : {
      "description" : "Include apps hidden with ⌘H.",
      "type" : "boolean"
    },
    "showMinimizedWindows" : {
      "description" : "Include minimized windows.",
      "type" : "boolean"
    },
    "showRecentlyClosed" : {
      "description" : "Offer recently closed apps in search so they can be reopened.",
      "type" : "boolean"
    },
    "showUnreadBadges" : {
      "description" : "Show each app's Dock unread badge on its entry.",
      "type" : "boolean"
    },
    "showWindowlessApps" : {
      "description" : "Include running apps that have no open window.",
      "type" : "boolean"
    },
    "showWindowStatusIcons" : {
      "description" : "Show the hidden, minimized, full-screen and no-window glyphs on each entry.",
      "type" : "boolean"
    },
    "showWindowTitleLabel" : {
      "description" : "Show each window's title, not just its app.",
      "type" : "boolean"
    },
    "sinkHiddenApps" : {
      "description" : "Move hidden apps to the bottom of the list instead of leaving them in place.",
      "type" : "boolean"
    },
    "sinkMinimizedWindows" : {
      "description" : "Move minimized windows to the bottom of the list instead of leaving them in their most-recently-used position.",
      "type" : "boolean"
    },
    "sortOrder" : {
      "description" : "Order apps and windows are listed in.",
      "enum" : [
        "mru",
        "mruWindows",
        "alphabetical",
        "launchOrder"
      ],
      "enumDescriptions" : [
        "Most recent",
        "Most recent (windows)",
        "Alphabetical",
        "Launch order"
      ],
      "type" : "string"
    },
    "soundOnCommit" : {
      "description" : "Play a sound when the switcher commits a selection.",
      "type" : "boolean"
    },
    "spaceScope" : {
      "description" : "Which Spaces windows are pulled from.",
      "enum" : [
        "allSpaces",
        "currentSpace",
        "visibleSpaces"
      ],
      "enumDescriptions" : [
        "All Spaces",
        "Current Space",
        "Visible Spaces"
      ],
      "type" : "string"
    },
    "stayOpenOnQuickTap" : {
      "description" : "Also keep it open after a quick tap of the shortcut.",
      "type" : "boolean"
    },
    "stayOpenOnRelease" : {
      "description" : "Keep the switcher open after the modifier is released.",
      "type" : "boolean"
    },
    "swipeCommitOnRelease" : {
      "description" : "Commit the swipe selection when the fingers lift.",
      "type" : "boolean"
    },
    "swipeMode" : {
      "description" : "What the three-finger swipe does.",
      "enum" : [
        "openSwitcher",
        "switchSpaces",
        "quickSwitch"
      ],
      "enumDescriptions" : [
        "Open switcher",
        "Switch Spaces",
        "Quick switch (last 2 apps)"
      ],
      "type" : "string"
    },
    "swipeReverseDirection" : {
      "description" : "Reverse the swipe direction.",
      "type" : "boolean"
    },
    "swipeSensitivity" : {
      "description" : "How far fingers must slide to advance one app. Higher = more sensitive.",
      "maximum" : 10,
      "minimum" : 1,
      "type" : "integer"
    },
    "tabDrillEnabled" : {
      "description" : "Peek the highlighted window's tabs with the Peek tabs key.",
      "type" : "boolean"
    },
    "titleRefreshIntervalMs" : {
      "description" : "Delay before window titles in the open switcher catch up after an app renames one (milliseconds). Lower reacts sooner, higher costs less CPU.",
      "maximum" : 2000,
      "minimum" : 50,
      "type" : "integer"
    },
    "titleTruncationMode" : {
      "description" : "Which part of a too-long title is replaced by an ellipsis.",
      "enum" : [
        "head",
        "middle",
        "tail"
      ],
      "enumDescriptions" : [
        "Beginning",
        "Middle",
        "End"
      ],
      "type" : "string"
    },
    "vimNavigationEnabled" : {
      "description" : "Navigate with h\/j\/k\/l as well as the arrow keys.",
      "type" : "boolean"
    },
    "windowDrillEnabled" : {
      "description" : "Peek the highlighted app's windows with ↓ while \"applications only\" is on.",
      "type" : "boolean"
    }
  },
  "title" : "BetterCmdTab configuration",
  "type" : "object"
}